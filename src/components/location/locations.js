import React from 'react';
import PropTypes from 'prop-types';
import Location from './Location';

function Locations({ name, temp }) {
  const arrayOfLocationTemps = loccationTemps.map((name, temp) => (
    return (
      <li>
        <Location name={name} temp={temp} />
      </li>
    )
  );
}
